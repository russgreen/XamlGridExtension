<UserControl x:Class="XamlGridExtension.ToolWindows.GridToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <TextBlock Grid.Row="0" Text="XAML Grid Helper"
               FontWeight="SemiBold" FontSize="13"
               Margin="8,8,8,4" />

    <!-- Grid info summary -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,0,8,4">
      <TextBlock x:Name="GridInfoText" Text="No Grid selected" Foreground="Gray" />
    </StackPanel>

    <!-- Visual grid display -->
    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Margin="8,4">
      <ItemsControl x:Name="GridVisual" />
    </ScrollViewer>

    <!-- Action buttons -->
    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="8,4,8,8" HorizontalAlignment="Left">
      <Button x:Name="InsertRowBtn"    Content="+ Row"    Margin="0,0,4,0" Padding="6,2" Click="InsertRowBtn_Click" />
      <Button x:Name="RemoveRowBtn"    Content="− Row"    Margin="0,0,4,0" Padding="6,2" Click="RemoveRowBtn_Click" />
      <Button x:Name="InsertColBtn"    Content="+ Col"    Margin="0,0,4,0" Padding="6,2" Click="InsertColBtn_Click" />
      <Button x:Name="RemoveColBtn"    Content="− Col"    Margin="0,0,4,0" Padding="6,2" Click="RemoveColBtn_Click" />
    </StackPanel>
  </Grid>
</UserControl>
